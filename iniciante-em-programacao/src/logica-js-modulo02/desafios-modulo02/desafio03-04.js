function calcularAreaEPerimetroDeSalaRetangular(altura, largura) {
    let area = altura * largura;
    let perimetro = (altura + largura) * 2;

    console.log(`A área da sala é ${area} m² e o perímetro é ${perimetro} m.`)
}

calcularAreaEPerimetroDeSalaRetangular(2, 3);